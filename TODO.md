- It's JSON currently, and loads entire JSON file for the user, very sad.
So maybe change from JSON to some database, and load only what user sees.
- Add translation
- Give context to the user what the topic is about.
- Give context to the LLM what the topic is about, and say that he has a reporter persona.
- Remove unnecessary stuff in article_text.
- Merge across dates too now, some get repeated a lot.

